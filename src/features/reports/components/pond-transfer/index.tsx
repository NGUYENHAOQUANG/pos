import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export const PondTransfer = () => {
  return (
    <View style={styles.container}>
      <Text>Pond Transfer Component</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    padding: 16,
  },
});
